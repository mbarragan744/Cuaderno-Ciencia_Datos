import tensorflow as tf
from tensorflow.keras import layers
model = tf.keras.Sequential(
    [layer.Input(shape=(32)),
    layers.Dense(64, activation='relu'),
    layers.Dense(64, activation='relu'),
    layers.Dense(10, activation='softmax')
])

model.compile( optimizer = 'adam', loss = 'sparse_categorical_crossentropy', metrics = ['accuracy']

model.fit(x_train, y_train, epochs = 10, batch_size = 32))

-----------------------------------------------------------------------------------------------------------


accuracy para clasificacion y error medio cuadratico cuando es para regresión

en los cuadernos esta todo, el parcial es el jueves 2 de abril


donde pongo/declaran las funciones de activacion? en cada capa y se ejecutan en cada neurona

prestar atencion al sigmoid, su salida va a estar entre 0 y 1, tanh esta entre -1 y 1, y ambos se utilizan para clasificacion

sigmoid-> cuando es por encima de 0.5 es igual a 1, cuando es por debajo de 0.5 entonces es 0. Se convirtió en una probabilidad

tanh-> si es -0.9 es resultado es 0, los resultados solo pueden ser 0 o 1

relu-> normalmente se utiliza en las capas intermedias, si se utiliza en la capa final, el valor del numero que va a predecir es numerico entero. Todo menor que 0 es 0, y por encima de 0 es lineal.

binomial se utiliza sigmoid
normalmente se utiliza relu

cuando los datos estan centrados en cero? cuando es normalizado con standarScaler

las salidas pueden ser binomiales o multiclases. si no es ninguna de las dos entonces es regresion

la perdida tiende a ser 0, la metrica tiende a ser 1 (accuracy)

regresion-> MSE

clasificacion binaria-> crossentropy

si no tiene a la salida funcion de activacion, es lineal. especificar eso a la IA


